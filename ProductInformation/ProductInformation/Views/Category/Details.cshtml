@model ProductInformation.Models.Category

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Category</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
@* This won't actually work here since this is for Products / Categories. Just an example. *@
<a href="/Category/Details?id=@(ViewBag.Book.ID)&action=extend">Extend</a>
<a href="/Category/Details?id=@(ViewBag.Book.ID)&action=delete">Delete</a>
<a href="/Category/Details?id=@(ViewBag.Book.ID)&action=return">Return</a>
